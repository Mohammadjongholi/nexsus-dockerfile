services:
  ubuntu-gui:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: ubuntu-gui

    ports:
      - "5901:5901"

    restart: unless-stopped

    environment:
      TZ: UTC

    volumes:
      - gui_home:/home/guiuser

volumes:
  gui_home:
